[project]
name = "oc-pruner"
version = "0.1.0"
description = "A tool for removing selected pieces of data from an OpenCitations metadata or citations table based on the table's validation report."
authors = [
    {name = "Elia Rizzetto",email = "elia.rizzetto@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
